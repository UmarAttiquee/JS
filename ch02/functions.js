// function fun(...num) {
//   let z = num.map((x) => x * 2);
//   return z;
// }

// let x = fun(1, 2, 3, 4, 5);
// console.log(x);

// ((name) => console.log(`Hello Word ${name}`))("Hamza");

let arr = [];
let obj = { 1: 1 };

if (arr.length === 0) {
  console.log("Arry is Empty");
} else {
  console.log("Array is not empty");
}

if (Object.keys(obj).length === 0) {
  console.log("Object is Empty");
} else {
  console.log("Object is not empty");
}
